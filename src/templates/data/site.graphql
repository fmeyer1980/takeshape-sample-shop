query {
  settings: getSiteSettings {
    siteTitle
     color {
      hex
    }
    headerStyle
  }
  collection: getCollectionList {
    total
    items {
      title
    }
  }
  faq: getFaq{
    title
  }
}
